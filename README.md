# Labell - Натяжні стелі в Києві

Сучасний, професійний веб-сайт для компанії Labell, що спеціалізується на натяжних стелях. Побудований на Next.js 14 з використанням TypeScript, Tailwind CSS та Framer Motion.

## 🚀 Технології

- **Next.js 14** - React фреймворк з App Router
- **TypeScript** - типізація коду
- **Tailwind CSS** - utility-first CSS фреймворк
- **Framer Motion** - бібліотека анімацій
- **React Hook Form** - управління формами
- **Zod** - валідація даних
- **Lucide React** - іконки

## 📁 Структура проекту

```
/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   ├── about/             # Сторінка "Про нас"
│   ├── contact/           # Сторінка контактів
│   ├── services/          # Сторінки послуг
│   ├── projects/          # Портфоліо
│   ├── calculator/        # Калькулятор вартості
│   ├── faq/               # FAQ сторінка
│   ├── layout.tsx         # Корневий layout
│   ├── page.tsx           # Головна сторінка
│   └── globals.css        # Глобальні стилі
├── components/            # React компоненти
│   ├── ui/               # UI компоненти (Button, Card, Modal, etc.)
│   ├── layout/           # Компоненти layout (Header, Footer)
│   ├── sections/         # Секції сторінок (Hero, Benefits, etc.)
│   ├── forms/            # Форми (ContactForm, CallbackForm)
│   ├── FAQ/              # FAQ компоненти
│   └── SEO/              # SEO компоненти
├── data/                  # Дані та конфігурація
│   ├── config.ts         # Конфігурація сайту
│   ├── services.ts       # Дані послуг
│   ├── projects.ts       # Дані проектів
│   ├── testimonials.ts   # Відгуки
│   └── faq.ts            # FAQ дані
├── lib/                   # Утиліти та допоміжні функції
│   ├── utils.ts          # Загальні утиліти
│   └── seo.tsx           # SEO утиліти
└── types/                 # TypeScript типи

```

## 🛠️ Встановлення та запуск

### Передумови

- Node.js 18+ 
- npm або yarn

### Крок 1: Встановлення залежностей

```bash
npm install
# або
yarn install
```

### Крок 2: Налаштування змінних оточення

Створіть файл `.env.local`:

```env
NEXT_PUBLIC_GA_ID=your-google-analytics-id
CRM_WEBHOOK_URL=your-crm-webhook-url
```

### Крок 3: Запуск розробки

```bash
npm run dev
# або
yarn dev
```

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

### Крок 4: Збірка для production

```bash
npm run build
npm start
```

## 📝 Налаштування контенту

### Зміна контактної інформації

Відредагуйте файл `data/config.ts`:

```typescript
export const siteConfig: SiteConfig = {
  contact: {
    phone: '+380501234567',
    email: 'info@labell.kiev.ua',
    // ...
  },
};
```

### Додавання проектів

Додайте новий проект у файл `data/projects.ts`:

```typescript
{
  id: 'unique-id',
  title: 'Назва проекту',
  slug: 'project-slug',
  type: 'glossy', // або 'matte', 'photoprint', etc.
  // ...
}
```

### Додавання послуг

Редагуйте файл `data/services.ts` для додавання або зміни послуг.

### Зміна FAQ

Оновіть дані у файлі `data/faq.ts`.

## 🎨 Кастомізація дизайну

### Зміна кольорів

Відредагуйте `tailwind.config.ts`:

```typescript
theme: {
  extend: {
    colors: {
      primary: {
        500: '#E5532A', // Ваш бренд-колір
      },
    },
  },
}
```

### Зміна шрифтів

Оновіть імпорти шрифтів у `app/layout.tsx`.

## 🔍 SEO

### Google Analytics

1. Отримайте GA ID
2. Додайте його у `.env.local` як `NEXT_PUBLIC_GA_ID`
3. Переконайтесь, що cookie consent налаштований

### Google Search Console

1. Додайте verification code у `app/layout.tsx`:
```typescript
verification: {
  google: 'your-verification-code',
}
```

2. Створіть `public/sitemap.xml` (можна автоматично через Next.js)

### Мета-теги та OG images

SEO налаштовано автоматично для кожної сторінки через `generateMetadata` функцію.

## 📧 Інтеграція форм

### Налаштування відправки форм

Відредагуйте API routes:

- `app/api/contact/route.ts` - форма контактів
- `app/api/callback/route.ts` - форма зворотного дзвінка

Додайте інтеграцію з вашою CRM/email сервісом:

```typescript
// Приклад інтеграції
await fetch(process.env.CRM_WEBHOOK_URL, {
  method: 'POST',
  body: JSON.stringify(formData),
});
```

## 🧪 Тестування

```bash
# Unit тести
npm run test

# E2E тести
npm run test:e2e

# Перевірка типів
npm run type-check

# Лінтер
npm run lint
```

## 🚀 Deployment

### Vercel (рекомендовано)

1. Підключіть репозиторій до Vercel
2. Додайте змінні оточення у налаштуваннях
3. Deploy автоматично виконується при push у main

### Інші платформи

```bash
npm run build
npm start
```

## 📚 Документація

- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Framer Motion](https://www.framer.com/motion/)

## 🤝 Підтримка

Для питань та підтримки зв'яжіться з командою розробки.

## 📄 Ліцензія

Всі права захищені © Labell 2024
